<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lqc.zufang.mapper.UserRoleMapper">
    <select id="listByUserId" parameterType="Integer" resultType="UserRole">
        select * from user_role where id=#{id}
    </select>
</mapper>